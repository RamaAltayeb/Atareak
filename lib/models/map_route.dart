class MapRoute {
  String status;
  dynamic json;
  dynamic launchPoint;
  dynamic destination;
  DateTime launchTime;
  
  MapRoute(
      {this.status,
      this.json,
      this.launchPoint,
      this.destination,
      this.launchTime});
}
